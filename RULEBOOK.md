# VCDP 项目规则书

## 1. 项目定位

VCDP 是一个轻量级本地单机工具，不是多人协同配置平台。

后续开发必须优先满足以下目标：

- 面向单个使用者在本机完成车辆通信设计工作。
- 默认不引入账号、登录、注册、角色、权限、租户、团队协作等平台化能力。
- 功能设计以简单、直接、可离线使用为优先。
- 数据默认保存在本机，避免依赖外部服务。
- 后端基于 Spring Boot 开发，前端保持轻量交互界面。

## 2. 技术基线

### 后端

- 使用 Java 21。
- 使用 Spring Boot 作为后端应用框架。
- 使用 MyBatis 作为数据访问层。
- 默认使用 H2 文件数据库作为本地持久化方案。
- 后端接口以本地 REST API 为主。

### 前端

- 使用 Vue 3。
- 使用 Vue Router 管理页面。
- 使用 Element Plus 构建基础界面。
- 不默认引入全局用户状态管理，除非后续确有非账号类全局状态需要。

## 3. 明确不做的方向

除非项目目标被重新定义，否则不要引入以下能力：

- 登录、注册、退出登录。
- JWT、Session、OAuth、SSO 等认证机制。
- 角色、权限、菜单权限、接口权限。
- 多用户、租户、组织、团队空间。
- 远程协同编辑。
- 云端同步作为核心依赖。
- 复杂微服务拆分。

如果某个功能看起来需要上述能力，必须先重新审视是否违背“本地单机工具”的定位。

## 4. 架构规则

- 保持单体应用结构，避免过早拆分模块或服务。
- 优先使用本地文件数据库和本地资源。
- 后端负责数据建模、校验、持久化和导入导出。
- 前端负责交互、展示和轻量编辑体验。
- API 设计应服务于本地工具流程，不为远程平台化预留过多复杂抽象。
- 新增领域能力时，优先围绕 Project、ECU、Interface、PDU、Signal 等车辆通信设计对象展开。

## 5. 数据规则

- 本地数据结构应清晰、可迁移、可导出。
- 数据库初始化脚本只维护本地工具需要的表和基础数据。
- 不创建用户、角色、权限相关表。
- 重要设计数据应考虑后续导入/导出能力。
- 避免把临时 UI 状态持久化为业务数据。

## 6. 后端开发规则

- Controller 保持薄层，只做请求接收、参数转换和响应返回。
- Service 承载业务规则和流程编排。
- Mapper 只负责数据访问，不承载业务判断。
- 公共响应结构继续使用 `Result`。
- 业务异常继续使用统一异常处理方式。
- 新增依赖前先确认是否真的必要，避免把轻量工具做重。
- 不新增 Spring Security、JWT 或权限框架。

## 7. 前端开发规则

- 应用打开后直接进入工具工作区。
- 页面结构围绕本地项目管理、通信对象编辑、导入导出等工具能力设计。
- 不出现登录页、注册页、用户头像、个人中心、退出登录等账号体系入口。
- 路由不做鉴权守卫。
- 前端状态优先放在局部组件中；只有跨页面共享且稳定的工具状态才考虑全局状态。
- UI 文案应明确表达“本地单机模式”。

## 8. 交互规则

- 操作路径应短，减少平台化流程。
- 用户应能快速进入编辑、查看、导入、导出等核心任务。
- 对本地数据的破坏性操作需要明确确认。
- 错误提示应面向工具使用场景，避免出现登录过期、权限不足等平台化文案。

## 9. 验证规则

每次功能变更后，优先执行：

```bash
mvn test
cd ui && npm run build
```

如果只修改文档，可不运行完整构建，但需要确认工作树变更清晰。

## 10. 后续演进原则

- 先完善核心车辆通信设计能力，再考虑周边增强。
- 先保证本地数据模型稳定，再扩展导入导出。
- 先做可用工具，再做复杂平台体验。
- 每次新增能力都要检查是否仍符合“轻量级本地单机工具”的定位。
